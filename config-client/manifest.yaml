---
applications:
  - name: config-client
    memory: 768M
    random-route: true
    path: target/config-client-0.0.1-SNAPSHOT.jar
    env:
      SPRING_PROFILES_ACTIVE: dev
    services:
      - limits-config-server